# 📋 Tasks — webar-demo
> `[ ]` open · `[>]` later · `[x]` done · _in progress = a live session lock (set automatically, not by hand)_

_(no tasks yet — add the first `- [ ] …` when work is identified)_
