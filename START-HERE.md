---
description: The generated map of webar-demo — what this repo is, where its code lives, and one line from each of its documents. Rebuilt by the pre-commit hook, so a wrong line is fixed in that document's own front matter and never here.
---

<!-- start-here:v1 — GENERATED by engine/start-here.mjs. DO NOT EDIT: the pre-commit hook rewrites it.
     Every line below is harvested from this repo. To change a description, edit that document's
     own `description:` front-matter — this file is the map, never the source. -->

# START HERE — webar-demo

**This repo does not say what it is.** Neither `spine/manifest.json` nor `README.md` carries a
name or a summary, so none is shown — inventing one here would make this file a source instead
of a map. Add it to whichever of those two files this repo keeps.

## Where the code lives

| directory | tracked files |
| --- | ---: |
| `design` | 12 |
| `src` | 10 |
| `(repo root)` | 5 |
| `backend` | 2 |
| `models` | 1 |
| `spine` | 1 |

## The documents

5 documents, grouped by folder. 1 carry a `description:` in their own front-matter; **4 do not** and appear as a bare path. A further 1 under archive/vendor folders are left off this map.

> A document with no `description:` cannot be routed to — you can only find it by already
> knowing the filename. That is 4 of this repo's docs, and the fix is one line in each.

### (repo root)

- `CLAUDE.md`
- `README.md`

### backend

- `backend/README.md`

### spine

- `spine/tasks.md`

### spine/wiki/technology

- **`spine/wiki/technology/kpix-lens-architecture.md`** — Production architecture for the KPIX LENS scan-and-identify AR app (CBS Bay Area lowrider parade) — modules, dedup strategy, cutout pipeline, scale plan

---

`[verified at …]` is the document's OWN `lastVerifiedCommit:` claim, harvested as written. Whether
the code has moved since is not computed here — that answer changes on every commit and would
rewrite this file forever. Ask `engine/drift.mjs`, which is allowed to be slow.
